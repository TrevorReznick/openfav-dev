---
import { twMerge } from 'tailwind-merge'
import {TYPOGRAPHY} from '~/config/styleCostants'

interface Props {
  title: string;
  value: string;
  change: string;
  type: 'increase' | 'decrease'
}

const { title, value, change, type } = Astro.props
---

<div class="overflow-hidden shadow rounded-lg bg-bgDark3">
  <div class="p-5">
    <div class="flex items-center">
      <div class="flex-1">
        <p class="text-sm font-medium text-gray-500 truncate">{title}</p>
        <p class="mt-1 text-3xl font-semibold text-white">{value}</p>
      </div>
    </div>
    <div class="mt-4">
      <div class={`flex items-center text-sm ${
        type === 'increase' ? 'text-green-600' : 'text-red-600'
      }`}>
        <span class="font-medium">{change}</span>
        <span class="ml-2">from last session</span>
      </div>
    </div>
  </div>
</div>