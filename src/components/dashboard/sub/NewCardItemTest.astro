---
import {Star, Link} from 'lucide-react' //common
import { Activity, Clock, List } from 'lucide-react' //activity
import { Bookmark } from 'lucide-react' //lastinsrtion
import { Folder, Bot, Book, FolderHeart } from 'lucide-react' //card list
import { Logs} from 'lucide-react' // personal activity
import GenericCard from '~/components/dashboard/sub/GenericCard.astro'

import type {GeneralCardItemType} from '~/types'

import { ExampleActivities } from '~/config/features'

const iconMap = {
    Activity: Activity, 
    Bot: Bot,
    Book: Book,
    Bookmark: Bookmark,
    Clock: Clock,   
    Folder: Folder,
    Link: Link,
    List: List,
    Logs: Logs,
    FolderHeart: FolderHeart,    
    Star: Star  
}

const lists = [
  { name: 'Progetti Web', description: 'Link utili per lo sviluppo web', icon: Folder },
  { name: 'Risorse AI', description: 'Articoli e tutorial sull\'intelligenza artificiale', icon: Bot },
  { name: 'Scholl Links', description: 'Links per produttività', icon: Book },
  // Altre liste...
]
---

<GenericCard: GeneralCardItemType
    card_name = "Test Card Name"
    card_icon = {inconMap[Book]}
    items={lists}
    title="Last Created Lists"
    icon={FolderHeart}
    itemText={(item) => `created the list "${item.name}"`}
    descriptionText="description"
    viewAllLink="/all-lists"
/>