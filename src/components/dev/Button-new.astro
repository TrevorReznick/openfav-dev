---
import { twMerge } from 'tailwind-merge'

const {
    classes = {}
} = Astro.props

const { inputs, action, title, text, button} = Astro.props




---
<!--
<section class={twMerge("flex flex-col items-center", containerClass)}>
-->


