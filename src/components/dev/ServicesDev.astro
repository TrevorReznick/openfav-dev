---
import Headline from './Headline.astro'

const { tagline, title, subtitle } = Astro.props

---
<Fragment >
    <Headline 
        title={title} tagline={tagline} subtitle={subtitle}
    />
    <slot/>
</Fragment>

    

