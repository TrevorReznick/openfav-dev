---
import { messageStore } from '~/store'
import Toast from '~/components/dev/ToastNotification.astro'

const {errorMessage = messageStore.get()} = Astro.props

---

<Toast errorMessage={errorMessage} />


